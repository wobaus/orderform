import { MenuItem } from './shared/menu-item.model';

export class Menus {
    private menu_items: MenuItem[] = [
        new MenuItem ('pancake', 10),
        new MenuItem ('chicken & waffle', 15)
    ];
}

