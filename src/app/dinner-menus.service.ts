import { MenuItem } from './shared/menu-item.model';

export class DinnerMenus {
    private menu_items: MenuItem[] = [
        new MenuItem ('lamb Shank', 28),
        new MenuItem ('Fish & Chip', 21)
    ];
}
